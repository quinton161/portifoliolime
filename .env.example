# Copy to ".env" in this folder, fill in values, restart `npm start` / rebuild.
# Never commit real keys — .env should stay gitignored.

# Best: deploy jarvis-backend with OPENAI_API_KEY on the server, then point the site here (no trailing slash).
# Local dev uses http://127.0.0.1:8000 automatically when this is unset.
# REACT_APP_ASSISTANT_API_URL=https://your-api.example.com

# --- Free-tier LLM keys (pick one; keys are inlined in the browser bundle — use backend in production) ---
# Google AI Studio (free): https://aistudio.google.com/apikey
REACT_APP_GEMINI_API_KEY=
REACT_APP_GEMINI_MODEL=gemini-2.0-flash

# Groq (free tier, fast): https://console.groq.com/keys
REACT_APP_GROQ_API_KEY=
REACT_APP_GROQ_MODEL=llama-3.3-70b-versatile

# Optional paid fallback — OpenAI
REACT_APP_OPENAI_API_KEY=
REACT_APP_OPENAI_MODEL=gpt-4o-mini

# Which provider: auto (Gemini → Groq → OpenAI) | gemini | groq | openai
# REACT_APP_LLM_PROVIDER=auto

# Contact form — create a free access key at https://web3forms.com (use the same email you want to receive messages).
# Submissions are emailed to that Web3Forms account inbox. Paste the key here; restart dev server after changes.
# Production: add the same variable in your host (e.g. Vercel → Environment Variables) and redeploy — CRA inlines env at build time.
# Without a key, submit falls back to opening the visitor's mail app.
REACT_APP_WEB3FORMS_ACCESS_KEY=
